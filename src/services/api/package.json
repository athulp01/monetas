{
  "scripts": {
    "build": "npx rollup -c rollup.worker.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "tslib": "^2.4.0",
    "wrangler": "^2.1.12"
  }
}
